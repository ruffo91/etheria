import pantalla

iniciar = pantalla.pantalla()
iniciar.start()



"""for evento in pygame.event.get():
    if evento.type == QUIT:
        salir()
    
    if evento.type == KEYDOWN and evento.key == K_ESCAPE and estado == "login":
        salir()
        
    if evento.type == KEYDOWN and (evento.key == K_DOWN or evento.key == K_s):
        escena.setAndando(True)
        escena.setO(0)
    if evento.type == KEYUP and (evento.key == K_DOWN or evento.key == K_s):
        escena.setAndando(False)"""''